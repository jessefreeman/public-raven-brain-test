# Memory retrieval field guide 2

## Context

This memory retrieval field guide records repeated memory retrieval observations so chunking, lexical search, and semantic recall all have enough material to work with. This memory retrieval field guide records repeated memory retrieval observations so chunking, lexical search, and semantic recall all have enough material to work with. This memory retrieval field guide records repeated memory retrieval observations so chunking, lexical search, and semantic recall all have enough material to work with. This memory retrieval field guide records repeated memory retrieval observations so chunking, lexical search, and semantic recall all have enough material to work with. This memory retrieval field guide records repeated memory retrieval observations so chunking, lexical search, and semantic recall all have enough material to work with. This memory retrieval field guide records repeated memory retrieval observations so chunking, lexical search, and semantic recall all have enough material to work with. This memory retrieval field guide records repeated memory retrieval observations so chunking, lexical search, and semantic recall all have enough material to work with. This memory retrieval field guide records repeated memory retrieval observations so chunking, lexical search, and semantic recall all have enough material to work with. This memory retrieval field guide records repeated memory retrieval observations so chunking, lexical search, and semantic recall all have enough material to work with. This memory retrieval field guide records repeated memory retrieval observations so chunking, lexical search, and semantic recall all have enough material to work with.

The overview anchor RB-LONG-0452-9E5Y stays near the top while the decisive late marker is saved for the final section.

## Decision

We kept the memory retrieval guidance in a single large note instead of splitting it, because the harness needs long memories that behave differently from short project notes. We kept the memory retrieval guidance in a single large note instead of splitting it, because the harness needs long memories that behave differently from short project notes. We kept the memory retrieval guidance in a single large note instead of splitting it, because the harness needs long memories that behave differently from short project notes. We kept the memory retrieval guidance in a single large note instead of splitting it, because the harness needs long memories that behave differently from short project notes. We kept the memory retrieval guidance in a single large note instead of splitting it, because the harness needs long memories that behave differently from short project notes. We kept the memory retrieval guidance in a single large note instead of splitting it, because the harness needs long memories that behave differently from short project notes. We kept the memory retrieval guidance in a single large note instead of splitting it, because the harness needs long memories that behave differently from short project notes. We kept the memory retrieval guidance in a single large note instead of splitting it, because the harness needs long memories that behave differently from short project notes.

## Open Questions

Should the search stack weight recent long-form evidence differently when the same memory retrieval appears across many shorter notes? Should the search stack weight recent long-form evidence differently when the same memory retrieval appears across many shorter notes? Should the search stack weight recent long-form evidence differently when the same memory retrieval appears across many shorter notes? Should the search stack weight recent long-form evidence differently when the same memory retrieval appears across many shorter notes? Should the search stack weight recent long-form evidence differently when the same memory retrieval appears across many shorter notes? Should the search stack weight recent long-form evidence differently when the same memory retrieval appears across many shorter notes? Should the search stack weight recent long-form evidence differently when the same memory retrieval appears across many shorter notes?

## Action Items

Validate that the tail marker remains searchable after chunking and that the result still outranks confusable notes sharing memory retrieval. Validate that the tail marker remains searchable after chunking and that the result still outranks confusable notes sharing memory retrieval. Validate that the tail marker remains searchable after chunking and that the result still outranks confusable notes sharing memory retrieval. Validate that the tail marker remains searchable after chunking and that the result still outranks confusable notes sharing memory retrieval. Validate that the tail marker remains searchable after chunking and that the result still outranks confusable notes sharing memory retrieval. Validate that the tail marker remains searchable after chunking and that the result still outranks confusable notes sharing memory retrieval. Validate that the tail marker remains searchable after chunking and that the result still outranks confusable notes sharing memory retrieval. Validate that the tail marker remains searchable after chunking and that the result still outranks confusable notes sharing memory retrieval.

Late retrieval marker: tail-anchor-0452-tmg44.